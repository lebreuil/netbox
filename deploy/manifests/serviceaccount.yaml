# NetBox workload service account. OpenBao access is provided separately to
# External Secrets Operator through the platform-managed netbox-secret-sync
# service account.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: netbox
  namespace: netbox